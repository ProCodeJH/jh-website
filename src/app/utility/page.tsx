import { UtilityStore } from "@/components/utility/utility-store";

export const metadata = {
  title: "Utility Store | JH",
  description: "실무에서 검증된 디지털 도구와 템플릿. 즉시 사용 가능.",
};

export default function UtilityPage() {
  return <UtilityStore />;
}
